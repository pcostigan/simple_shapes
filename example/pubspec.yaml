name: shapes_example
description: "Simple shapes with theme control."
#
version: 1.0.1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  simple_shapes: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0


flutter:
  uses-material-design: true
